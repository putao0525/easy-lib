//
// Created by putao on 2024/9/2.
//
///数字类型
#ifndef EASY_NUM_MIN
#define EASY_NUM_MIN(a, b) ((a) < (b) ? (a) : (b))
#endif

#ifndef EASY_NUM_MAX
#define EASY_NUM_MAX(a, b) ((a) < (b) ? (a) : (b))
#endif
